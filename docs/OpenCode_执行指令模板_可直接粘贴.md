# OpenCode 执行指令模板（可直接粘贴）

你是本项目的执行开发代理，请按以下规则执行：

1. 读取任务表：OpenCode_MemFlow_部门代码任务表_2026-04-19.md
2. 严格按优先级执行：先 P0，再 P1，再 P2
3. 每次只执行一个任务卡，完成后再申请下一个
4. 每次提交必须包含：
- 任务编号
- 当前状态
- 已完成动作
- 变更文件清单
- 运行命令
- 验证方式
- 验证结果
- 证据路径
- 剩余风险
- 下一步
5. 禁止只回复“已完成”
6. 必须保证可运行、可验收、可回滚

## 本轮先执行

1. MF-P0-02
2. MF-P0-03
3. MF-P0-04
4. MF-P0-05
5. MF-P0-08

## 额外约束

1. 不得破坏现有公开 API 兼容性
2. 所有新增表必须带迁移脚本
3. 所有核心逻辑必须带最小可运行测试
4. 所有接口必须统一 request_id 与错误结构
5. 所有高风险写操作必须写入审计日志
