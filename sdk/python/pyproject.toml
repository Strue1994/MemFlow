[project]
name = "memflow-sdk"
version = "0.1.0"
description = "MemFlow Python SDK"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
